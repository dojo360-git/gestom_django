créé la page statistiques_heures : 
(tu peux t'inspirer de statistiques_collecte/)

on peut saisir :
date_debut 
date_fin 

le bouton filtrer permet de filtrer les date de début et de fin 

utilise la requete suivante: 
    sql = """
        SELECT
            id_agent,
            date,
            id_stat,
            type,
            id_flux,
            id_itineraire,
            is_heures_sup,
            hr_debut,
            hr_fin,
            motif_hs,
            presence_id,
            stat_planning,
            nom,
            employeur,
            qualification,
            service,
            background_color,
            border_color
        FROM stat_heures
        WHERE (date BETWEEN date_debut AND date_fin ) 
        ORDER BY id_agent, date, stat_planning;
    """


premier tableau : le nombre d'absence par pres : where presence_id is not null

count(*) / pres    relie avec presence_id de la table core_presencemotif 
